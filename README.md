# network-capture-pro-chrome-extension-
Chrome extension (Manifest V3) that captures every network request on any tab — full URLs, headers, cookies, query params, POST bodies, response headers, Set-Cookie, response bodies (text + binary) — and exports the whole session as ZIP, JSON, HAR or CSV in one click. Zero dependencies, 100% local, no telemetry.
